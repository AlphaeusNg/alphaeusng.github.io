# Private financial data - never commit the raw tracker
data/Networth Tracker.xlsx
*.xlsx

# Python
__pycache__/
*.py[cod]
*$py.class

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Seeking Biblical Truth integration (Task 4)
# Cloned locally via `git clone --depth 1` for localhost testing and Obsidian vault browsing.
# The vault contents are NOT committed to this site repo (separate source of truth).
# This keeps the site repo lean; users run the clone command locally for experimentation.
seeking-biblical-truth/

# Obsidian vault caches & volatile files (large or session-specific; exclude even if folder present)
**/.obsidian/workspace.json
**/.obsidian/workspace*
**/.obsidian/cache/
**/.obsidian/trash/
**/.obsidian/backup/
**/.obsidian/log/
*.obsidian.log
